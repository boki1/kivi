dist: trusty
language: cpp
compiler: gcc

os: linux
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-9

script:
  - export CC=gcc-9
  - export CXX=g++-9
  - cmake .
  - cmake --build . -- -j2
  - ctest -j2
