# Removes files generated during compilation
rm -rf build

# Removes files which are automatically generated
rm -rf src/autogen/kivi_parser/*

# Removes the files from the previous documentation build
rm -rf docs/build
